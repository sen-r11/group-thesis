# The .xml and .jsonl readers need nothing. These cover the other inputs.

python-evtx>=0.7.4      # .evtx files                    -> parse.py from_evtx
pywin32>=306            # the live channel, Windows only -> parse.py from_live
scikit-learn>=1.3.0     # the metrics                    -> Evaluation/harness.py

# Install with:  pip install -r requirements.txt
#
# Run the parser, from Framework/:
#   python parse.py "name of Sysmon .xml file" --json events.jsonl
#
# Run the detection engine, from Framework/:
#   python -m Detection.engine events.jsonl --json result.json
#
# Run the evaluation harness, from Framework/:
#   python -m Evaluation.harness Evaluation/corpus.csv
