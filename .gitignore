__pycache__/
*.pyc
.venv/
venv/

# never commit raw Sysmon exports or samples
*.evtx
*.zip
logs/

# the malware itself, and what came out of detonating it. captures hold
# real host paths and user names, and samples must never leave the VM.
samples/
captures/

# what build.py produces
build/
dist/
*.spec
live_alerts.jsonl

# engine output, not source
/Framework/result.json

.VSCodeCounter/**
